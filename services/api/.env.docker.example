# Copy to .env for local Docker PostgreSQL development if desired.
# Keep secrets out of git; the real .env is ignored.

POSTGRES_DB=tokad_mart
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# When the API itself runs inside Docker Compose later, use:
# POSTGRES_HOST=postgres
